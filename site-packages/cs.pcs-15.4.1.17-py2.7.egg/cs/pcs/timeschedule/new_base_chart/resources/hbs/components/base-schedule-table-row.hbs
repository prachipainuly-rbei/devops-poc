<div class="schedule-table-column order" {{bind-attr title=viewingContent.index}}>
  {{viewingContent.index}}
</div>
{{#each column in settings.columns}}
  {{base-table-cell columnBinding='column'
                    chartType=chartType
                    contentBinding='viewingContent'
                    refTableBinding='refTable'
                    settingsBinding='settings'}}
{{/each}}
