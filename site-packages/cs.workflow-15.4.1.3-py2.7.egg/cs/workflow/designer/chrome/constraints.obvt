<div class="title-icon">
    <img class="svg-icon" data-src="${icon}"/>
</div>
<a class="btn btn-mini pull-right constraints-action" data-if="addConstraintUrl">
    <img data-src="${options.plugins}catalog/catalog.png" />
</a>
<div class="title-text text-ellipsis muted" title="${label}">${label}</div>

<ul class="unstyled">
    <li data-repeat="constraints" data-render="@."></li>
</ul>
