var path = require('path');

module.exports = {
    entry: './demo.js',
    output: {
        //path: path.join(__dirname, 'build'),
        filename: 'demo-transpiled.js',
        //libraryTarget: 'umd',
        //library: 'TreeExample'
    },
    devtool: 'source-map',
    module: {
        rules: [
            {test: /\.jsx?$/, exclude: /node_modules/, loader: 'eslint-loader', enforce: 'pre'},
            { test: /\.jsx?$/, exclude: /(node_modules|bower_components)/,
              loader: 'babel-loader',
              query: {presets: ['react', 'es2015']}
            },
            { test: /\.css$/, loader: "style-loader!css-loader" },
            { test: /\.svg(\?v=\d+\.\d+\.\d+)?$/, loader: "url?limit=10000&minetype=image/svg+xml"},
            { test: /\.gif$/, loader: "url-loader?limit=8192" },
            { test: /\.png$/, loader: "url-loader?limit=8192" },
            { test: /\.json$/, loader: "json-loader" }
        ]
    },
    externals: {
        // See https://gist.github.com/STRML/2117f574726bdf0b8d58
        // if we just do 'moment': 'moment' we have problems using this
        // package as library
        "jquery": "jQuery",
        "bootstrap": "bootstrap",
        "react": "React",
        "react-dom": "ReactDOM",
        "react-bootstrap": "ReactBootstrap"
    },
    resolve: {
        extensions: ['', '.js', '.jsx'],
        alias: {
            "cs-vp-utils": path.resolve('../../utils')
        }
    }
};
