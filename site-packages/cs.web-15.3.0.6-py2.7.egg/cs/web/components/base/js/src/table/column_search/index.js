/*
 * Copyright (C) 2016 CONTACT Software GmbH
 * All rights reserved.
 * http://www.contact-software.com
 *
 * Revision "$Id: index.js 148148 2016-10-25 08:19:30Z yzh $"
 */

import actions from './actions';
import reducers from './reducers';
import ToolbarButton from './ToolbarButton';
import SearchHeaderColumnAction from './ColumnActions';
import Provider from './Provider';

/**
 * Provides columns search functionality in a table.
 *
 * Components
 * ----------
 *
 * - Provider: Is required for column search
 * - ColumnActions: Adds a search input control (FormControl), if configured for the column, to the column header
 * - ToolbarButton: Is required for column search.
 *                  Adds a button to show/hide column header search input controls
 *                  Adds a button to excecute search if search input controls are visible
 *
 * Properties
 * ----------
 *
 * +----------------------+----------------+---------+-----------------------------------------------------------------+
 * | Property             | Type           | Default | Use                                                             |
 * +======================+================+=========+=================================================================+
 * | initURL              | string         | -       |  TODO: equal to initURL from ConfiguredForm                     |
 * +----------------------+----------------+---------+-----------------------------------------------------------------+
 * | initValues           | Immutable.Map  | -       |  Values to initial fill search input controls                   |
 * +----------------------+----------------+---------+-----------------------------------------------------------------+
 * | onSearchHeaderSubmit | func           | -       |  Callback to start search operation                             |
 * +----------------------+----------------+---------+-----------------------------------------------------------------+
 *
 * Remarks
 * -------
 * TODO:
 *
 * @module
 */

export default {
    actions,
    Provider,
    reducers,
    columnActions: [SearchHeaderColumnAction],
    ToolbarButton,
    toolbarButtons: [ToolbarButton]
};
