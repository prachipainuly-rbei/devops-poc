/*
 * Copyright (C) 2017 CONTACT Software GmbH
 * All rights reserved.
 * http://www.contact-software.com
 *
 * Revision "$Id: LinkEntity.jsx 174679 2018-03-19 11:49:45Z ssa $"
 */

import React from 'react';
import {PropTypes} from 'cs-web-components-externals';

export default class LinkEntity extends React.Component {
    render() {
        const {contentState, entityKey, children} = this.props;
        const {href} = contentState.getEntity(entityKey).getData();
        return (
            <a href={href} target="_blank">
                {children}
            </a>
        );
    }
}

LinkEntity.propTypes = {
    contentState: PropTypes.object.isRequired,
    entityKey: PropTypes.string.isRequired
};
