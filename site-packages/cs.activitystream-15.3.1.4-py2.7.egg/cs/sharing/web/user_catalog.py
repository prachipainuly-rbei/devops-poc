from cdb.platform import gui
from cdb.objects.org import User


class UserCatalog(gui.CDBCatalog):
    def handleSelection(self, selected_objects):
        objects = [User._FromObjectHandle(so) for so in selected_objects]
        self.setValue('system:description', ';'.join([obj.GetDescription() for obj in objects]))
