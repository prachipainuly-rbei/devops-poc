DOCID Directory
===============

This directory contains generated mappings from the installed
documentation to the helpids.

To regenerate this files, run the cdb.helptools.<xxxx> script.

The important files:
   catalogue.db         -  An sqlite DB that lists all doc targets
   redirect.conf        -  The main redirect file for apache
  

